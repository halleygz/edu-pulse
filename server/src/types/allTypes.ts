export interface UserTypes {
    full_name: string;
    phone_number: string;
    email: string;
    password: string;
    username: string;
}

export interface loginTypes {
    email: string;
    password: string;
}